import React from 'react';
import ReactDOM from 'react-dom';
import Entry from './Components/index';
import './index.less';

const wrapper = document.getElementById('app');
ReactDOM.render(<Entry />, wrapper);
